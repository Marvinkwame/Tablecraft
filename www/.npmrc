# fumadocs-core@16.x optionally peers on zod@4.x, while @marvinackerman/tablecraft
# needs zod@3.x. The docs site works fine on zod 3; this tells npm (and Vercel's
# install step) to use legacy peer resolution instead of failing with ERESOLVE.
legacy-peer-deps=true
